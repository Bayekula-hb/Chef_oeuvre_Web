import {makeStyles} from '@mui/styles';

const divContainerStyles = makeStyles(() => ({
  div: {
      width: '100%',
      height:'100%',
      display:"flex",
      flexDirection: 'row',
  },
}));

export default divContainerStyles;