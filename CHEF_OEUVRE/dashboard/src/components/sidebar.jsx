import {styled} from "@mui/styles";

const SideBar = ()=>{
    const SideBar = styled("div")(()=>({
        width:"10vw",
        display:"flex",
        flexDirection:"row",
    }));
    return(
        <SideBar>

        </SideBar>
    )
}
export default SideBar;