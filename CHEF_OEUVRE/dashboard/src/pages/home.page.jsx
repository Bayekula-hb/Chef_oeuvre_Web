import {styled, makeStyles} from "@mui/material";
const Home = ()=>{
    const HomeContainer = styled("div")(()=>({
        display:"flex",
    }))
    return(
        <HomeContainer>
            
        </HomeContainer>
    )
}
export default Home;